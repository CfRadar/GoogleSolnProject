/**
 * App.tsx
 * Root component — mounts the hash router.
 * All page rendering is delegated to Router.
 */
import Router from "./router";

function App() {
  return <Router />;
}

export default App;
